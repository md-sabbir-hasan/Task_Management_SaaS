# Task_Managemen# Task Management API

A RESTful API for managing tasks with JWT-based user authentication, built with FastAPI and MySQL.

## Features

- User registration and login with JWT authentication
- Password hashing with bcrypt
- Full CRUD operations for tasks (Create, Read, Update, Delete)
- Protected endpoints (authentication required)
- Auto-generated interactive API documentation (Swagger UI)
- MySQL database integration with SQLAlchemy ORM

## Tech Stack

- **Framework:** FastAPI
- **Database:** MySQL
- **ORM:** SQLAlchemy
- **Authentication:** JWT (python-jose), bcrypt (passlib)
- **Server:** Uvicorn
- **Validation:** Pydantic

## Setup Instructions

1. Clone the repository
2. Create a virtual environment:t_Saas